---
import { TRIVIA_CATEGORIES, QUIZ_LIMITS } from "../types";
import LoadingSpinner from "./LoadingSpinner.astro";
---

<div class="max-w-2xl mx-auto bg-white rounded-lg shadow-lg p-8">
  <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">
    Configure Your Quiz
  </h2>

  <form id="quiz-config-form" class="space-y-6">
    <div>
      <label for="amount" class="block text-sm font-medium text-gray-700 mb-3">
        Number of Questions
      </label>

      <div class="text-center mb-4">
        <div
          class="inline-flex items-center justify-center w-20 h-20 bg-indigo-100 rounded-full border-4 border-indigo-200 transition-all duration-200 hover:border-indigo-300 hover:bg-indigo-200"
        >
          <span
            id="amount-display"
            class="text-3xl font-bold text-indigo-800 transition-all duration-200"
          >
            {QUIZ_LIMITS.DEFAULT_QUESTIONS}
          </span>
        </div>
        <p class="text-sm text-gray-600 mt-2">
          You will answer <span
            id="amount-text"
            class="font-semibold text-indigo-600 transition-all duration-200"
            >{QUIZ_LIMITS.DEFAULT_QUESTIONS}</span
          > questions
        </p>
      </div>

      <input
        type="range"
        id="amount"
        name="amount"
        min={QUIZ_LIMITS.MIN_QUESTIONS}
        max={QUIZ_LIMITS.MAX_QUESTIONS}
        value={QUIZ_LIMITS.DEFAULT_QUESTIONS}
        class="w-full h-3 bg-gray-200 rounded-lg appearance-none cursor-pointer slider focus:outline-none focus:ring-2 focus:ring-indigo-500"
      />
      <div class="flex justify-between text-sm text-gray-500 mt-2">
        <span>{QUIZ_LIMITS.MIN_QUESTIONS}</span>
        <span>{QUIZ_LIMITS.MAX_QUESTIONS}</span>
      </div>
    </div>

    <div>
      <label
        for="category"
        class="block text-sm font-medium text-gray-700 mb-2"
      >
        Category
      </label>
      <select
        id="category"
        name="category"
        class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500"
      >
        <option value="">Any Category</option>
        {
          TRIVIA_CATEGORIES.map((category) => (
            <option value={category.id}>{category.name}</option>
          ))
        }
      </select>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">
        Difficulty
      </label>
      <div class="flex space-x-4">
        <label class="flex items-center">
          <input
            type="radio"
            name="difficulty"
            value=""
            checked
            class="form-radio h-4 w-4 text-indigo-600"
          />
          <span class="ml-2 text-sm text-gray-700">Any</span>
        </label>
        <label class="flex items-center">
          <input
            type="radio"
            name="difficulty"
            value="easy"
            class="form-radio h-4 w-4 text-indigo-600"
          />
          <span class="ml-2 text-sm text-gray-700">Easy</span>
        </label>
        <label class="flex items-center">
          <input
            type="radio"
            name="difficulty"
            value="medium"
            class="form-radio h-4 w-4 text-indigo-600"
          />
          <span class="ml-2 text-sm text-gray-700">Medium</span>
        </label>
        <label class="flex items-center">
          <input
            type="radio"
            name="difficulty"
            value="hard"
            class="form-radio h-4 w-4 text-indigo-600"
          />
          <span class="ml-2 text-sm text-gray-700">Hard</span>
        </label>
      </div>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">
        Question Type
      </label>
      <div class="flex space-x-4">
        <label class="flex items-center">
          <input
            type="radio"
            name="type"
            value=""
            checked
            class="form-radio h-4 w-4 text-indigo-600"
          />
          <span class="ml-2 text-sm text-gray-700">Any</span>
        </label>
        <label class="flex items-center">
          <input
            type="radio"
            name="type"
            value="multiple"
            class="form-radio h-4 w-4 text-indigo-600"
          />
          <span class="ml-2 text-sm text-gray-700">Multiple Choice</span>
        </label>
        <label class="flex items-center">
          <input
            type="radio"
            name="type"
            value="boolean"
            class="form-radio h-4 w-4 text-indigo-600"
          />
          <span class="ml-2 text-sm text-gray-700">True/False</span>
        </label>
      </div>
    </div>

    <button
      type="submit"
      class="w-full bg-indigo-600 text-white py-3 px-4 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 transition-colors font-medium"
    >
      Start Quiz
    </button>
  </form>

  <div id="loading-state" class="hidden">
    <LoadingSpinner message="Loading your quiz..." />
  </div>

  <div
    id="error-state"
    class="hidden bg-red-50 border border-red-200 rounded-md p-4 mt-4"
  >
    <div class="flex">
      <div class="flex-shrink-0">
        <svg
          class="h-5 w-5 text-red-400"
          viewBox="0 0 20 20"
          fill="currentColor"
        >
          <path
            fill-rule="evenodd"
            d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z"
            clip-rule="evenodd"></path>
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-red-800">Error</h3>
        <p id="error-message" class="mt-1 text-sm text-red-700"></p>
        <button
          id="retry-button"
          class="mt-2 text-sm bg-red-600 text-white px-3 py-1 rounded hover:bg-red-700"
        >
          Try Again
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  import { QuizConfigManager } from "../scripts/quizConfig";

  window.addEventListener("DOMContentLoaded", () => {
    new QuizConfigManager();
  });
</script>
